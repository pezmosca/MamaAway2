<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toni.pruebatabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrickMarket">
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".ListaHome" />
        <activity android:name=".TareasHome" />
        <activity android:name=".CalendarioHome" />
        <activity android:name=".AvisoHome" />
        <activity android:name=".ListaPersonal" />
        <activity android:name=".Tutorials"></activity>
    </application>

</manifest>